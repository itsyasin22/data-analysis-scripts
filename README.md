# A simple Python script to calculate the area of a square
dle
  khhs:
        s
        se-99
    """ikl:

# e uf the f= 5e_aeade)
    print(f"The rea of a squa side gt
    # Another exampl
    result2 = calculate_sqre_area(side2)
    print(f"The area of a square with a side len
